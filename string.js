const name = "aman kumar"
const repoCount = 50

// console.log(name +  repoCount + " value"); it is old methode

console.log(`Namste mera naam ${name} hai aur mera repocount ${repoCount} hai`);//it is the new way of the syntax 

const gameName = new String('amankr');
console.log(gameName);
console.log(gameName[0]);//way to access the key value of the string.
console.log(gameName.__proto__);//syntax to access the object

console.log(gameName.length);
console.log(gameName.toUpperCase());//it converts the whole string into the uppercase 

console.log(gameName.charAt(5));//it is use to find on the given position which char is stands

const newstringOne = "     amit    ";
console.log(newstringOne);
console.log(newstringOne.trim());//{.trim()} removes all the spaces from the string