/**(>) is less than
 * (<) is grater than
 * (==) is equal to 
 * (===) is strictly equal to 
 * (!=) is not equal to 
 * (>=) is less than or equal to
 * (<=) is greater than or equal to
 */

// dangerous comparision 
console.log(null > 0);
console.log(null == 0);
console.log(null >= 0);

console.log(undefined == 0);
console.log(undefined > 0);
console.log(undefined < 0);

// ===
console.log("3" === 5);